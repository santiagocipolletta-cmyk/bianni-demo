'use client'

// Re-uso de la página admin/contenido pero con header propio
import AdminContenidoPage from '@/app/(admin)/admin/contenido/page'

export default function MarketingBibliotecaPage() {
  return <AdminContenidoPage />
}
